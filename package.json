{
  "scripts": {
    "build": "node build.js",
    "start": "npm run build",
    "dev": "npm run build"
  },
  "dependencies": {
    "discord.js": "^14.25.1",
    "fs": "^0.0.1-security",
    "web": "^0.0.2",
    "windows": "^0.1.2"
  }
}
